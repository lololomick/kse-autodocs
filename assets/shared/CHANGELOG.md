# Changelog

Automated per-commit log of changes to this codebase. New entries are appended by the documentation pipeline (see the `Auto-update changelog` step in the CI configuration).

Each entry records the commit SHA (short), author name and email, date, and a short human-readable summary of what changed in that commit.

<!-- pipeline:insert-below -->
